<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="File">
	<select id="selectFileSeq" resultType="int">
		SELECT FILE_NO_SEQ.NEXTVAL FROM DUAL
	</select>
	<insert id="insertFile" parameterType="java.util.HashMap">
		insert into CRD_FILE_INFO  (file_id, file_name, File_save_name, file_path,file_size)
							 values(#{file_id},#{file_name},#{file_save_name},#{file_path},#{file_size})
	</insert>
</mapper>